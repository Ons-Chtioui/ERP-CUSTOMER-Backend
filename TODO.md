# TODO

## Planned fixes
1. Fix `src/common/filters/http-exception.filter.ts` compile/type issues in the global exception filter.
2. Fix `src/app.module.ts` syntax/brace errors and configuration typos (mailer port key) so the app builds.
3. Run `pnpm build` (and optionally `pnpm test`) to verify no TypeScript errors remain.

- [x] Step 1: Fix `src/common/filters/http-exception.filter.ts` compile/type issues.


